import { MainUserHighlightPipe } from './main-user-highlight.pipe';

describe('MainUserHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new MainUserHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
